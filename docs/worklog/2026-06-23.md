# 작업 기록 — 2026-06-23 (veggieverse-frontend)

## 요약
- **PR 2건 머지**: `#74`(오전), `#76`(오후) — 모두 `develop → main`
- **커밋 14개**, 누적 **43 files / +2,041 / -873**
- 영역: 마이페이지(회원탈퇴/탈퇴유예), 스토어(품절표시), 비밀번호 재설정, 결제 실패 UX, Error Boundary, 보안·취약점, about 재구성

---

## A. 오전 — PR #74 (10:57 ~ 12:14)

| 커밋 | 내용 | 변경 |
|---|---|---|
| `df938e6` | feat(mypage): 회원탈퇴 API 연동 (#60) | 3 files, +63/-16 |
| `ec058be` | feat: 레시피·뉴스레터·구독 페이지 준비중 오버레이 추가 | 4 files, +93/-12 |
| `fe9cec5` | feat(store): 품절 상품 표시 개선 — 디밍 막 + 좌상단 SOLD OUT 배지 | 1 file, +24/-17 |
| `229aaff` | chore(nav): 카탈로그 링크 도메인 변경 (slunch-catalog.vercel.app → catalogue.slunch.co.kr) | 1 file, +2/-2 |
| `5a51fe9` | feat(mypage): 탈퇴 유예(PENDING_DELETION) 상태 감지 + 복구 모달 (#60) | 7 files, +207/-14 |
| `256b0cd` | style(store): 품절 표시 미세조정 — 막 더 밝게 + SOLD OUT 태그 축소 | 1 file, +5/-5 |
| `da22a41` | fix(subscribe): 메뉴 상세 모달 로고 중복 제거 | 1 file, +2/-31 |

**머지**: `e452037` Merge PR #74

---

## B. 오후 — PR #76 (14:27 ~ 15:23)

### B-1. 비밀번호 재설정 (이메일) — 신규 · `b85fa26`
> feat(auth): 비밀번호 재설정(이메일) 구현 + 커스텀 메일 템플릿 — 6 files, +402/-87

- Supabase PKCE 흐름으로 기존 mock 제거하고 실제 동작 구현
- `requestPasswordResetAction` (resetPasswordForEmail, 계정 열거 방지)
- `/auth/callback`에서 `next` 파라미터 처리로 recovery 흐름 분기
- `/auth/update-password` 페이지 신설 (세션 확인 → updateUser, 만료/에러 처리)
- find-password: 이메일 탭 실제 동작 / 휴대폰 탭 "준비 중"
- 브랜드 메일 템플릿 `docs/email-templates/reset-password.html`
- **보류**: 휴대폰 인증 비번재설정 (SMS provider + 백엔드 전화번호→계정 API 필요)

### B-2. 결제 실패 UX (구독 흐름) · `4704744`
> feat(payment): 구독 결제 실패 UX 개선 — 2 files, +221/-28

- `postPayment`가 `null` 대신 원인별 `SubscriptionPaymentError`를 던지도록 변경
  (Retryable / Rejected / Conflict / PlanInvalid / Auth / Unknown)
- `alert()` 제거 → 인페이지 에러 패널로 원인별 안내 + 재시도/식단 재구성
- (참고: store 주문 흐름은 기존에 이미 잘 처리돼 있었음)

### B-3. Error Boundary + 404 · `6e42029`
> feat(error): 라우트 Error Boundary + 404 페이지 추가 — 8 files, +323

- 공통 폴백 `ErrorView` 컴포넌트
- `global-error`(레이아웃 붕괴 대비) / 루트 `error` / `not-found`
- order · subscribe · cart · mypage 라우트별 맞춤 `error.tsx`
- 모든 바운더리에서 `Sentry.captureException` 연동

### B-4. 보안 · 정합성 후속 패치 · `b3309f8`
> fix: 보안·정합성 점검 후속 패치 — 3 files, +48/-5

- `auth/callback`: `next` 검증(`safeNextPath`)으로 오픈 리다이렉트 하드닝
- subscribe/order: 프로필 로드 실패 시 안내 + 다시 불러오기 (기존엔 빈 폼만 노출)
- payment: 401 분류 dead code 제거

### B-5. 카트 레이스 · find-id · 취약점 · `5803041`
> fix: 카트 동기화 레이스·find-id mock 정리 + 의존성 취약점 0건화 — 4 files, +291/-416

- CartContext: `hydrated` 플래그를 복원·병합 완료 후로 이동 (로그인 동기화 시 게스트 카트 유실 방지)
- find-id: mock alert 제거 → "준비 중" 안내 + 로그인/비밀번호 찾기 동선
- deps: `next 16.2.9` / `react·react-dom 19.2.7` / `@tosspayments 2.7.1`
- `pnpm.overrides`로 `@opentelemetry/core ≥2.8.0`, `@babel/core ≥7.29.6` 강제
  → `pnpm audit`: **No known vulnerabilities found**

### B-6. about 페이지 재구성 · `204da8c`
> feat(about): about 페이지 싱글 롱스크롤로 재구성 (1차) (#67) — 8 files, +368/-248

**머지**: `79d33aa` Merge PR #76

---

## 점검 결과 (오후 보안·버그 감사)
의존성 취약점 3건(모두 간접 의존성) → overrides로 0건화. 코드 감사에서 에이전트가
CRITICAL로 보고한 일부(프록시 X-Auth-Mode 스푸핑, SSRF)는 검증 결과 **실제 취약점 아님**으로
판정, 실제 수정이 필요한 항목만 B-4/B-5에 반영.

## 배포 전 수동 설정 (코드 외)
- [ ] Supabase **Redirect URLs**에 `/auth/callback` 등록 (운영/로컬) — *완료됨*
- [ ] Supabase **Reset Password 이메일 템플릿** 적용 — *완료됨*
- [ ] 운영 트래픽 대비 **Custom SMTP** 연결 (기본 SMTP는 발송 한도 있음) — *미완*

## 후속 과제
- (예정 2026-06-24) UX 고도화: 로딩 스켈레톤 / React Query 도입 / 코드 스플리팅
- 휴대폰 인증 비번재설정 (SMS + 백엔드 API)
- find-id 실제 구현 (동일 백엔드 의존)
- 기존 `react-hooks/set-state-in-effect` lint 부채 정리 (UserContext 등, 34건)

## 검증
- `tsc --noEmit` 통과 / `pnpm build` 성공 (Next 16.2.9 · React 19.2.7) / 변경 파일 lint clean
